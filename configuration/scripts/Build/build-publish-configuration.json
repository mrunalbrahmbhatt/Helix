{
  "__comment":
    "This configuration file is used by the build-publish-local.ps1 script which builds and publishes projects included in Solution to file system.",
  "config": {
    "slnName": "Helix.sln",
    "destinationDir": "D:\\TestDeploy",
    "segmentMarker": "code",
    "filesToExclude": [ ".Test.csproj", "NamespacePrefix.ModuleType.ModuleName.csproj", "NamespacePrefix.ModuleType.ModuleName.Tests.csproj", "Scripts.pssproj" ] 
  }
}
